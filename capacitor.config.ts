import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'invoice-sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
